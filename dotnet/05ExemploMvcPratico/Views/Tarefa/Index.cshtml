@model IEnumerable<_05ExemploMvcPratico.Models.Tarefa>

@{
    ViewData["Title"] = "Listagem de contatos";
}

<h2> Tarefas</h2>


<p> <a asp-action="Criar" class="btn btn-primary">Nova Tarefa</a></p> 

<table class="table">
    <thead>
        <tr>
            <th>
                @* @Html.DisplayNameFor(model =>model.Id) *@
                ID
            </th>
            <th>
                @* @Html.DisplayNameFor(model =>model.Nome) *@
                TÍTULO
            </th>
            <th>
                @* @Html.DisplayNameFor(model =>model.Telefone) *@
                DESCRIÇÃO
            </th>
            <th>
                @* @Html.DisplayNameFor(model =>model.Ativo) *@
                DATA
            </th>
            <th>
                @* @Html.DisplayNameFor(model =>model.Status) *@
                STATUS
            </th>
        </tr>
    </thead>

    @* <tbody>
        @foreach (var item in Model)
        {
            <tr>
              <td>
                @Html.DisplayFor(model => item.Id)
              </td>
              <td>
                @Html.DisplayFor(model => item.Nome)
              </td>              
              <td>
                @Html.DisplayFor(model => item.Telefone)
              </td>
              <td>
                @Html.DisplayFor(model => item.Ativo)
              </td>
              <td>
                <a asp-action="Editar" class="btn btn-primary" asp-route-id="@item.Id">Editar</a>
                <a asp-action="Detalhes" class="btn btn-secondary" asp-route-id="@item.Id">Detalhes</a>
                <a asp-action="Deletar" class="btn btn-danger" asp-route-id="@item.Id">Deletar</a>
              </td>
            </tr>
        }
    </tbody> *@
</table>